---
# defaults file for host_inspector

module_library_path: "{{ playbook_dir }}/.test"
module_tasks_path: "{{ playbook_dir }}/.test"
module_log_path: "{{ playbook_dir }}/logs"
# List of applications to check
apps_to_check:
  - curl
  - git
  - docker
  - ufw

# List of apis to test
apis_to_test: []
  # - name: "ollama"
  #   url: "http://127.0.0.1"
  #   port: 11434
  #   endpoint: "/"
  #   expected_result: "Ollama is running"
  # - name: "prometheus"
  #   port: 9090
  #   endpoint: "/-/healthy"
  # - name: "grafana"
  #   port: 3000
  #   endpoint: "/api/health"

# Timeout for api tests
api_test_timeout: 10
