[defaults]
# inventory = inventory  # Path to your inventory file
roles_path = ../roles  # Where Ansible will look for roles
host_key_checking = False  # Disable SSH host key checking
retry_files_enabled = False  # Disable creation of retry files

# [ssh_connection]
# ssh_args = -o ControlMaster=auto -o ControlPersist=30m -o UserKnownHostsFile=/dev/null  # SSH options
# pipelining = True  # Pipelining reduces the number of SSH operations required

# [privilege_escalation]
# become = True
# become_method = sudo
# become_user = root  # Or the appropriate user with sudo privileges
# become_ask_pass = no  # If you don't want to be prompted for sudo password

# [connection]
# pipelining = True

# [inventory]
# enable_plugins = host_list, virtualbox, yaml  # Enable inventory plugins

# [colors]
# highlight = white
# verbose = yellow
# debug = dark gray
# deprecate = purple
# skip = cyan
# unreachable = red
# ok = green
# changed = blue
# # diff_add...

# # Something went wrong, please try again.